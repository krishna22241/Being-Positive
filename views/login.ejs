<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Form</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      .form-container {
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        padding: 20px;
        width: 300px;
        text-align: center;
        margin: auto; /* Center the form horizontally */
        margin-top: 39px;
        margin-bottom: 39px; /* Adjusted top margin to center vertically */
      }

      .form-container form {
        display: flex;
        flex-direction: column;
      }

      .form-container label {
        margin-bottom: 8px;
      }

      .form-container input,
      .form-container select {
        padding: 8px;
        margin-bottom: 10px;
        border: 1px solid #ccc;
        border-radius: 4px;
      }

      .form-container button {
        background-color: #4caf50;
        color: #fff;
        padding: 10px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
      }

      .form-container button:hover {
        background-color: #45a049;
      }

      .form-container .toggle-link {
        margin-top: 10px;
        color: #007bff;
        cursor: pointer;
        text-decoration: underline;
      }

      /* Hide the login form initially */
      #lregisterForm {
        display: none;
      }

      /* Style for the back-to-register link */
      .back-to-register {
        color: #007bff;
        cursor: pointer;
        text-decoration: underline;
        margin-top: 10px;
      }
    </style>
  </head>
  <body>
    <div class="form-container" id="loginForm">
      <h2>Login</h2>
      <form action="/api/v1/login" method="post">
        <label for="login-email">Email:</label>
        <input type="email" id="login-email" name="email" required />

        <label for="login-password">Password:</label>
        <input type="password" id="login-password" name="password" required />

        <label for="login-role">Role:</label>
        <input type="text" name="role" id="role" value="donor" readonly />

        <button type="submit">Login</button>

        <a href="/reg">
          <p class="back-to-register">New user? Register here</p>
        </a>
      </form>
    </div>

  </body>
</html>
